{
    "version": 2,
    "functions": {
      "timeout": 20
    },
    "name": "new-resume-backend",
    "builds": [{ "src": "server.js", "use": "@vercel/node" }],
    "routes": [{ "src": "/(.*)", "dest": "/server.js" }]
  }